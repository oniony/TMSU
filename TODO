MUST

* When performing 'delete' or 'untag' that leave file with no tags then remove it from the database.
* Auto-create database.
* Handle modified files.
* Handle long filenames when making link name.
* Tagging, untagging and removal via VFS paths.
* Status command
    >./tmsu status
    ? somefile.blah
    ? blahdeblah
    M hello
        blah,greeting
    U fred 
        person,blah,tree
* Clean-up command
* VFS dates
* Tag aliasing.
* File removal when all tags removed.

MAYBE

* Handle SIGINT.
