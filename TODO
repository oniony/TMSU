MUST

* Use tag directory size to list number of entries.
* Use date of real file.
* Auto-create database on start.
* Handle long filenames when making link name.
* Tagging, untagging and removal via VFS paths.
* Status command
    >./tmsu status
    ? somefile.blah
    ? blahdeblah
    M hello.txt: blah, greeting, hello
    U fred.jpg: person, blah, tree
* Tag aliasing.

MAYBE

* Handle SIGINT.
